name: Project ENV
on: workflow_dispatch
concurrency:
  group: ${{github.ref}}
  cancel-in-progress: true
jobs:
  console_env:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: 获取当前ENV配置[secrets.WORK_WECHAT_GITHUB_ROBOT_KEY]
        run: echo ${{secrets.WORK_WECHAT_GITHUB_ROBOT_KEY}}
