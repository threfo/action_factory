name: Project ENV
on: workflow_dispatch
concurrency:
  group: ${{github.ref}}
  cancel-in-progress: true
jobs:
  console_env:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: 获取当前ENV配置[secrets]
        run: cat ${secrets}
